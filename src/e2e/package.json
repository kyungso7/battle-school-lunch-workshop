{
  "name": "battle-school-lunch-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "pretest": "npm --prefix ../web run build",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
