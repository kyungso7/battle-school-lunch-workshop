# 급식 배틀 - 학교 급식 조회 앱

## 1. 문서 목적

이 문서는 NEIS 공개 API를 활용한 학교 급식 조회 웹 애플리케이션의 제품 요구사항을
정의한다. 제품의 범위와 사용자 경험에 대한 단일 기준이며, 구체적인 기술 구현은
`TRD.md`에서 정의한다.

## 2. 제품 개요

### 2.1 개발 목적

학생, 학부모, 교직원이 학교 이름과 날짜만으로 원하는 학교의 중식 식단을 빠르게
확인할 수 있도록 한다. 사용자가 NEIS 기관 코드나 교육청 코드를 알 필요 없이 학교를
검색하고, 최대 31일의 날짜 범위에 해당하는 급식을 날짜별로 확인하는 단순한 흐름을
제공한다.

### 2.2 대상 사용자

- 본인 또는 자녀의 학교 급식을 확인하려는 학생과 학부모
- 학교 급식 일정을 확인하려는 교직원
- 여러 학교의 급식 정보를 탐색하려는 일반 사용자

### 2.3 핵심 가치

- 학교 이름 일부만 입력해도 원하는 학교를 찾을 수 있다.
- 학교 선택부터 급식 확인까지 세 단계 안에 완료할 수 있다.
- 날짜별 중식 메뉴와 관련 정보를 읽기 쉬운 형태로 제공한다.
- 외부 서비스 오류나 데이터 부재를 사용자가 이해할 수 있는 상태로 안내한다.

## 3. 제품 범위

### 3.1 포함 범위

- 학교 이름 일부를 이용한 학교 검색
- 검색 결과에서 학교 식별에 필요한 정보 표시 및 학교 선택
- 시작일과 종료일을 이용한 날짜 범위 선택
- 선택한 학교와 날짜 범위의 중식 조회
- 날짜별 급식 메뉴 및 NEIS가 제공하는 부가 정보 표시
- 로딩, 빈 결과, 입력 오류, 서비스 오류 상태 안내
- 데스크톱과 모바일 웹 브라우저에서 사용할 수 있는 반응형 UI

### 3.2 제외 범위

- 조식과 석식 조회
- 로그인, 회원 가입 및 사용자별 데이터 저장
- 학교 또는 급식 즐겨찾기
- 급식 메뉴 수정, 등록 또는 삭제
- 급식 품질 평가, 학교 간 비교 및 AI 분석
- 푸시 알림과 급식 일정 구독
- NEIS에 없는 과거 데이터의 자체 수집 및 보관

## 4. 핵심 사용자 흐름

### 4.1 1단계: 학교 검색 및 선택

1. 사용자가 학교 이름의 일부를 입력한다.
2. 사용자가 검색을 실행한다.
3. 시스템은 입력값과 일치하는 학교 목록을 표시한다.
4. 사용자는 학교명, 학교 종류, 지역 또는 주소를 확인하고 한 학교를 선택한다.
5. 시스템은 선택한 학교를 유지하고 날짜 선택 단계로 이동한다.

### 4.2 2단계: 날짜 범위 선택

1. 사용자가 조회 시작일과 종료일을 선택한다.
2. 시스템은 시작일이 종료일보다 늦지 않고 조회 기간이 최대 31일인지 검증한다.
3. 유효한 범위가 선택되면 사용자는 급식 조회를 실행한다.

### 4.3 3단계: 날짜별 급식 결과 표시

1. 시스템은 선택한 학교와 날짜 범위의 중식 정보를 조회한다.
2. 결과는 날짜가 빠른 순서로 구분하여 표시한다.
3. 각 날짜에는 급식일, 식사 구분, 메뉴를 기본으로 표시한다.
4. NEIS 응답에 값이 있으면 칼로리, 영양 정보, 원산지 정보도 함께 표시한다.
5. 사용자는 학교 또는 날짜를 변경해 다시 조회할 수 있다.

## 5. 기능 요구사항

### FR-01 학교 검색

- 학교명 검색어는 앞뒤 공백을 제거한 뒤 처리한다.
- 빈 검색어로 검색할 수 없다.
- 학교 이름 전체가 아닌 일부만 입력해도 검색할 수 있어야 한다.
- 검색 중임을 알 수 있는 상태를 표시하고 중복 요청을 방지한다.
- 검색 결과에는 최소한 학교명, 학교 종류, 시도명 또는 주소를 표시한다.
- 같은 이름의 학교를 구분할 수 있도록 충분한 식별 정보를 제공한다.

### FR-02 학교 선택

- 검색 결과 중 하나만 선택할 수 있다.
- 선택 상태를 시각적으로 명확하게 표시한다.
- 급식 조회에는 선택한 학교의 교육청 코드와 행정표준코드를 사용한다.
- 학교를 변경하면 기존 급식 결과가 새 선택과 혼동되지 않도록 초기화한다.

### FR-03 날짜 범위 선택

- 시작일과 종료일을 모두 입력해야 한다.
- 시작일은 종료일보다 늦을 수 없다.
- 시작일과 종료일을 포함한 조회 범위는 최대 31일이다.
- 날짜는 사용자가 이해하기 쉬운 형식으로 표시하되 서버와의 통신에서는 명확한
  날짜 형식을 사용한다.

### FR-04 급식 조회

- 급식 조회 전에 학교와 유효한 날짜 범위가 선택되어 있어야 한다.
- 식사 구분은 중식으로 고정한다.
- 조회 중임을 알 수 있는 상태를 표시하고 중복 요청을 방지한다.
- 결과를 날짜별로 구분하고 일관된 순서로 표시한다.
- 메뉴에 포함된 알레르기 표기 등 NEIS 원문 정보는 의미가 훼손되지 않게 표시한다.

### FR-05 상태 유지와 재조회

- 날짜 범위를 변경해도 선택한 학교를 유지한다.
- 급식 결과 화면에서 학교 검색 또는 날짜 선택 단계로 돌아갈 수 있다.
- 입력을 변경한 뒤 다시 조회하면 최신 조건의 결과만 표시한다.

### FR-06 접근성과 반응형 UI

- 입력 요소에 명확한 레이블과 오류 설명을 제공한다.
- 키보드만으로 검색, 선택, 날짜 입력, 조회를 수행할 수 있어야 한다.
- 상태와 오류는 색상에만 의존하지 않고 텍스트로도 전달한다.
- 주요 흐름은 모바일과 데스크톱 화면에서 가로 스크롤 없이 사용할 수 있어야 한다.

## 6. 예외 및 오류 처리

| 상황 | 기대 동작 |
| --- | --- |
| 검색어가 비어 있음 | 검색을 요청하지 않고 학교명을 입력하라는 메시지를 표시한다. |
| 학교 검색 결과가 없음 | 조건에 맞는 학교가 없음을 알리고 다른 검색어를 안내한다. |
| 학교를 선택하지 않음 | 날짜 선택 또는 급식 조회를 진행하지 않고 학교 선택을 안내한다. |
| 날짜가 누락됨 | 시작일과 종료일을 모두 선택하도록 안내한다. |
| 시작일이 종료일보다 늦음 | 잘못된 날짜 범위임을 설명하고 조회를 요청하지 않는다. |
| 조회 범위가 31일을 초과함 | 최대 31일까지 조회할 수 있음을 알리고 조회를 요청하지 않는다. |
| 급식 정보가 없음 | 오류 화면 대신 해당 기간에 등록된 중식이 없음을 표시한다. |
| 네트워크 또는 서버 오류 | 이해하기 쉬운 오류 메시지와 재시도 수단을 제공한다. |
| NEIS 서비스 오류 또는 시간 초과 | 일시적인 외부 서비스 문제임을 안내하고 재시도를 허용한다. |

## 7. 비기능 요구사항

### 7.1 성능

- 일반적인 네트워크 환경에서 사용자의 요청에 즉시 진행 상태를 표시한다.
- 학교 검색과 급식 조회는 필요한 데이터만 요청하며 불필요한 중복 요청을 피한다.
- 외부 API 응답 지연이 전체 UI를 멈추게 하지 않는다.

### 7.2 신뢰성

- 외부 API의 오류와 빈 결과를 구분해 처리한다.
- 이전 요청보다 늦게 도착한 응답이 최신 화면 상태를 덮어쓰지 않아야 한다.
- 외부 API 계약과 다른 응답을 정상 데이터로 표시하지 않는다.

### 7.3 보안 및 개인정보

- NEIS API 키를 브라우저에 노출하지 않는다.
- 사용자의 검색어나 선택 정보를 영구 저장하지 않는다.
- 사용자에게 외부 API의 내부 오류 세부 정보나 비밀 값을 노출하지 않는다.

### 7.4 호환성

- 최신 주요 데스크톱 및 모바일 브라우저에서 핵심 흐름을 제공한다.
- 화면 크기와 입력 방식이 달라도 동일한 기능을 사용할 수 있어야 한다.

## 8. 성공 기준

- 사용자가 학교명 일부로 원하는 학교를 검색하고 선택할 수 있다.
- 유효한 최대 31일의 날짜 범위를 선택해 중식을 조회할 수 있다.
- 조회 결과가 날짜별로 구분되어 표시된다.
- 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음이 서로 다른 상태로 안내된다.
- 브라우저에 NEIS API 키가 전달되거나 NEIS API가 직접 호출되지 않는다.
- 학교 검색부터 급식 결과 확인까지의 전체 흐름이 자동화된 E2E 테스트로 검증된다.

## 9. 인수 조건

- 앱 제목과 개발 목적, 대상 사용자가 문서와 화면에 일관되게 반영된다.
- 학교 검색, 날짜 범위 선택, 중식 결과 표시의 3단계 흐름이 구현된다.
- 부분 학교명 검색, 단일 학교 선택, 최대 31일 날짜 범위 검증을 지원한다.
- 날짜별 중식 결과와 명시적인 로딩, 빈 결과 및 오류 상태를 제공한다.
- 조식과 석식은 조회 결과에 포함하지 않는다.
- 프런트엔드는 백엔드를 통해서만 데이터를 조회한다.
