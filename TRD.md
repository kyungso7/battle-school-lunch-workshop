# 급식 배틀 - 기술 요구사항 및 설계

## 1. 문서 목적

이 문서는 `PRD.md`의 제품 요구사항을 구현하기 위한 기술 구조, 시스템 경계, 데이터
흐름, API 원칙, 배포 구성 및 테스트 전략을 정의한다.

## 2. 설계 원칙

- 프런트엔드, 백엔드, 외부 NEIS API의 책임과 계약을 분리한다.
- 브라우저는 NEIS API를 직접 호출하지 않고 백엔드의 `/api/*`만 호출한다.
- NEIS API 키는 백엔드 런타임에서만 관리한다.
- 외부 NEIS 계약은 저장소의 NEIS OpenAPI 명세를 기준으로 구현한다.
- 프런트엔드와 백엔드 사이의 내부 계약도 OpenAPI 문서로 명시하고 양쪽 타입의
  기준으로 사용한다.
- 외부 서비스 실패와 데이터 없음은 서로 다른 결과로 처리한다.
- 로컬 개발과 컨테이너 실행에서 동일한 애플리케이션 동작을 유지한다.

## 3. 시스템 구성

```text
사용자 브라우저
  │
  │ HTTPS/HTTP, /api/*
  ▼
React 프런트엔드
  │
  │ 동일 오리진 프록시
  ▼
Python FastAPI 백엔드
  │
  │ HTTPS + NEIS_API_KEY
  ▼
NEIS 오픈 API
  ├── /schoolInfo
  └── /mealServiceDietInfo
```

애플리케이션은 React 기반 Web 서비스와 Python 기반 API 서비스로 구성한다. 개발
환경에서는 Vite가 `/api`를 API 서버로 프록시하고, 컨테이너 환경에서는 Web
컨테이너의 nginx가 동일 오리진 `/api` 요청을 API 컨테이너로 전달한다.

## 4. API 계약 기준

### 4.1 외부 NEIS 계약

- 이슈에서 지정한 `data/openapi.json`을 NEIS 학교기본정보 및 급식식단정보 계약의
  원본으로 취급한다.
- 현재 저장소에서 명세가 루트 `openapi.json`에 존재하는 경우 해당 파일은 같은 외부
  계약의 작업 사본으로 사용하며, 구현 시 사본 간 차이가 없어야 한다.
- 백엔드는 명세의 경로, 필드명, 필수 여부, 응답 구조를 임의로 변경하지 않는다.
- NEIS 전용 클라이언트만 외부 경로 `/schoolInfo`와
  `/mealServiceDietInfo`를 호출한다.
- 외부 요청은 JSON 형식을 사용하고 인증 키, 타임아웃, 페이지 크기 등 공통 값을
  중앙에서 관리한다.

### 4.2 내부 애플리케이션 계약

- 프런트엔드와 백엔드 간 엔드포인트 및 데이터 스키마는 `src/openapi.json`으로
  정의한다.
- `src/openapi.json`은 NEIS 원본 응답을 그대로 노출하는 계약이 아니라 제품 UI에
  필요한 필드와 오류 형식을 명확히 한 내부 계약이다.
- 백엔드 Pydantic 모델과 프런트엔드 TypeScript 타입은 내부 계약과 일치해야 한다.
- 타입 불일치를 `any` 또는 무분별한 타입 단언으로 우회하지 않는다.

## 5. 프런트엔드 설계

### 5.1 기술 스택

- React 19
- TypeScript
- Vite
- Tailwind CSS v4
- TanStack React Query
- Vitest, React Testing Library, MSW

### 5.2 책임

- 학교 검색, 학교 선택, 날짜 범위 선택, 결과 표시의 3단계 UI 제공
- 사용자 입력의 즉각적인 검증과 접근 가능한 오류 표시
- `/api/*` 내부 API 호출 및 서버 상태 관리
- 로딩, 빈 결과, 오류, 재시도 상태 표시
- 응답 데이터를 날짜별 중식 카드 또는 목록으로 표현

### 5.3 제약

- 컴포넌트와 페이지에서 NEIS API를 직접 호출하지 않는다.
- NEIS API 키를 환경 변수, 정적 파일 또는 브라우저 번들에 포함하지 않는다.
- 모든 백엔드 호출은 단일 API 클라이언트 모듈을 통한다.
- 서버 데이터는 React Query로 관리하고 중복 요청, 오래된 응답 및 재시도 정책을
  일관되게 적용한다.

### 5.4 UI 상태 모델

1. `school-search`: 검색어 입력과 검색 결과 표시
2. `date-selection`: 선택 학교 요약과 시작일/종료일 입력
3. `meal-results`: 조회 조건 요약과 날짜별 결과 표시

각 단계는 `idle`, `loading`, `success`, `empty`, `error` 상태를 명시적으로 구분한다.
학교가 바뀌면 날짜 선택은 유지할 수 있지만 기존 급식 결과는 폐기한다. 급식 조회
중 조건이 변경되면 이전 응답이 새 조건의 화면을 덮어쓰지 않도록 요청 키 또는 취소
기능을 사용한다.

## 6. 백엔드 설계

### 6.1 기술 스택

- Python 3.12 이상
- FastAPI
- Pydantic 및 pydantic-settings
- 비동기 httpx
- Uvicorn
- pytest 및 respx

### 6.2 책임

- 내부 `/api/*` 계약 제공
- 요청 파라미터와 날짜 범위 검증
- NEIS 인증 키와 외부 연결 설정 관리
- 외부 OpenAPI 계약에 맞춘 NEIS 요청 및 응답 파싱
- NEIS 응답을 내부 API 스키마로 변환
- 외부 오류, 시간 초과, 잘못된 응답 및 데이터 없음의 정규화

### 6.3 모듈 경계

```text
app/
├── main.py              # 앱 팩토리, 수명 주기, CORS, 라우터 등록
├── config.py            # 환경 설정과 비밀 값 주입
├── schemas.py           # 내부 요청 및 응답 Pydantic 모델
├── neis_client.py       # 유일한 NEIS HTTP 호출 경계
└── routers/
    ├── health.py
    ├── schools.py
    └── meals.py
```

라우터는 HTTP 입력과 출력에 집중하고, NEIS URL이나 원본 응답 구조를 직접 다루지
않는다. NEIS 클라이언트는 비동기 방식으로 호출하고 연결 및 전체 요청 타임아웃을
명시한다.

## 7. 내부 API 설계

정확한 필드와 예시는 구현 단계의 `src/openapi.json`에서 확정하되 다음 경계를
유지한다.

### 7.1 상태 확인

`GET /api/health`

- 서비스의 요청 처리 가능 여부를 반환한다.
- API 키나 외부 서비스의 민감한 진단 정보를 노출하지 않는다.

### 7.2 학교 검색

`GET /api/schools?query={부분 학교명}`

- `query`는 공백을 제거한 비어 있지 않은 문자열이어야 한다.
- 백엔드는 NEIS `/schoolInfo`의 `SCHUL_NM` 조건으로 검색한다.
- 응답 항목에는 최소한 교육청 코드, 학교 행정표준코드, 학교명, 학교 종류, 시도명
  또는 주소가 포함된다.
- 검색 결과 없음은 성공한 빈 목록으로 반환한다.

### 7.3 급식 조회

`GET /api/meals?officeCode={교육청코드}&schoolCode={학교코드}&from={YYYY-MM-DD}&to={YYYY-MM-DD}`

- 학교 식별 코드, 시작일, 종료일은 필수다.
- 시작일은 종료일보다 늦을 수 없다.
- 양 끝 날짜를 포함한 범위는 최대 31일이다.
- 백엔드는 NEIS `/mealServiceDietInfo` 호출 시 날짜를 `YYYYMMDD`로 변환하고
  `MMEAL_SC_CODE=2`를 강제한다.
- 응답은 날짜 오름차순의 중식 목록이며 급식일, 메뉴, 식사 구분을 필수로 포함한다.
- 칼로리, 영양 정보, 원산지는 NEIS 응답에 있을 때 선택적으로 포함한다.
- 급식 정보 없음은 성공한 빈 목록으로 반환한다.

### 7.4 오류 형식

모든 내부 API 오류는 최소한 다음 의미를 일관되게 제공한다.

- 안정적인 오류 코드
- 사용자에게 노출 가능한 메시지
- 필요한 경우 입력 필드 정보
- 요청 추적을 위한 식별자

권장 HTTP 상태는 잘못된 입력 `422`, 외부 서비스 오류 `502`, 외부 시간 초과 `504`,
예기치 않은 서버 오류 `500`이다. NEIS 원본 오류, 스택 추적, API 키는 응답에
포함하지 않는다.

## 8. 데이터 흐름

### 8.1 학교 검색

1. 사용자가 프런트엔드에 부분 학교명을 입력한다.
2. 프런트엔드 API 클라이언트가 `GET /api/schools`를 호출한다.
3. 백엔드가 입력을 검증하고 NEIS 클라이언트에 검색을 위임한다.
4. NEIS 클라이언트가 `/schoolInfo`를 호출해 응답을 검증한다.
5. 백엔드가 외부 필드를 내부 학교 스키마로 변환한다.
6. 프런트엔드가 결과 또는 빈 상태를 표시한다.

### 8.2 급식 조회

1. 사용자가 학교와 날짜 범위를 확정한다.
2. 프런트엔드가 `GET /api/meals`를 호출한다.
3. 백엔드가 학교 코드와 최대 31일 범위를 검증한다.
4. NEIS 클라이언트가 `MMEAL_SC_CODE=2`와 날짜 범위로
   `/mealServiceDietInfo`를 호출한다.
5. 백엔드가 외부 응답을 내부 급식 스키마로 변환하고 날짜순으로 정렬한다.
6. 프런트엔드가 날짜별 결과, 빈 상태 또는 오류를 표시한다.

## 9. 설정 및 보안

- `NEIS_API_KEY`는 백엔드 환경 변수로만 주입한다.
- CORS 허용 목록은 `CORS_ORIGINS`로 제한하고 와일드카드를 사용하지 않는다.
- 로그에 API 키, 전체 외부 요청 URL의 인증 파라미터 또는 민감한 응답을 기록하지
  않는다.
- 외부 API 호출은 HTTPS만 사용한다.
- 운영 컨테이너는 non-root 사용자, 최소 권한, `cap_drop: ALL`,
  `no-new-privileges`, read-only 루트 파일시스템을 적용한다.
- 외부 응답은 신뢰하지 않고 Pydantic 모델로 검증한 뒤 사용한다.

## 10. Docker Compose 구성

### 10.1 서비스

| 서비스 | 역할 | 외부 노출 |
| --- | --- | --- |
| `web` | React 정적 자산 제공 및 `/api` 역방향 프록시 | `${WEB_PORT:-8080}` |
| `api` | FastAPI 내부 API 및 NEIS 연동 | Compose 네트워크 내부만 |

### 10.2 실행 원칙

- 루트 `compose.yaml`에서 Web과 API 이미지를 각각 빌드하고 실행한다.
- Web은 API의 health check가 준비된 후 요청을 전달할 수 있어야 한다.
- API 키는 Compose 설정에 직접 작성하지 않고 루트 `.env` 또는 배포 환경의 비밀
  주입 기능을 사용한다.
- 브라우저에는 Web 포트만 노출하고 API 컨테이너 포트는 호스트에 공개하지 않는다.
- nginx는 동일 오리진 `/api`를 API 서비스로 전달하고 그 외 경로에서는 SPA
  라우팅을 지원한다.
- 두 서비스는 종료 신호를 정상 처리하고 health check를 제공한다.

## 11. 테스트 전략

모든 자동화 테스트는 실제 NEIS 서비스와 실제 API 키에 의존하지 않는다.

### 11.1 프런트엔드 통합 테스트

프런트엔드는 구현 세부 단위 테스트 대신 사용자 흐름 중심의 통합 테스트만 작성한다.
MSW로 `/api/*`를 모킹하고 다음을 검증한다.

- 부분 학교명 입력, 검색 결과 표시 및 학교 선택
- 검색 결과 없음과 검색 오류 상태
- 날짜 범위 필수값, 역전 범위 및 31일 초과 검증
- 급식 조회 로딩과 날짜별 중식 결과 표시
- 급식 정보 없음, API 오류 및 재시도
- 학교 변경 시 기존 급식 결과 초기화

### 11.2 백엔드 단위 테스트

HTTP I/O 없이 다음 순수 동작을 검증한다.

- 날짜 형식 변환과 최대 31일 범위 검증
- NEIS 학교 및 급식 응답의 내부 스키마 변환
- 중식 필터와 날짜 정렬
- NEIS 오류 코드의 내부 오류 매핑
- 설정 검증과 민감 값 비노출

### 11.3 백엔드 통합 테스트

FastAPI 테스트 클라이언트와 respx로 NEIS HTTP 경계를 모킹하고 다음을 검증한다.

- `/api/health`, `/api/schools`, `/api/meals`의 요청 및 응답 계약
- NEIS 요청 파라미터, 인증, 중식 코드 및 날짜 형식
- 빈 NEIS 결과의 성공한 빈 목록 변환
- 잘못된 입력, 외부 오류, 시간 초과 및 잘못된 응답 처리
- API 키나 외부 오류 세부 정보가 응답에 노출되지 않음

### 11.4 E2E 테스트

Playwright에서 `/api/*`를 안정적으로 모킹하고 브라우저 전체 흐름을 검증한다.

1. 학교 이름 일부를 입력한다.
2. 결과에서 학교를 선택한다.
3. 유효한 날짜 범위를 선택한다.
4. 조회를 실행한다.
5. 날짜별 중식 결과를 확인한다.

별도 시나리오로 학교 없음, 잘못된 날짜 범위, 급식 없음 및 서버 오류를 검증한다.
locator는 role과 label을 우선하며 테스트는 실행 순서와 외부 네트워크에 독립적이어야
한다.

## 12. 품질 기준

- 내부 API 구현과 양쪽 타입이 `src/openapi.json` 계약을 충족한다.
- 프런트엔드 빌드와 타입 검사가 성공한다.
- 프런트엔드 통합 테스트, 백엔드 단위 및 통합 테스트, E2E 테스트가 성공한다.
- Docker Compose 구성 검증과 두 서비스의 health check가 성공한다.
- 컨테이너 환경에서 학교 검색부터 중식 결과 확인까지 전체 흐름이 동작한다.
- 브라우저 네트워크 요청과 빌드 결과에 NEIS API 키가 포함되지 않는다.

## 13. 기술적 비범위

- 데이터베이스와 영구 캐시
- 사용자 인증 및 권한 관리
- NEIS 데이터의 배치 수집
- 학교 간 급식 비교와 AI 분석
- Azure 배포 리소스 및 CI/CD 파이프라인
- MCP 서버와 멀티 에이전트 워크플로우
