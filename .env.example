# Copy this file to .env and replace the API-key placeholder before running Compose.
NEIS_API_KEY=replace-with-your-neis-api-key

# The only host-exposed service is the web application.
WEB_PORT=8080

# Keep browser origins explicit; comma-separated values are supported when needed.
CORS_ORIGINS=http://localhost:8080
